# WhisperX-MLX - WhisperX with MLX acceleration for Apple Silicon
# Installed from GitHub since it's not on PyPI
git+https://github.com/sooth/whisperx-mlx.git@c5f2c06b51c199c0ad4e992853a43fdae361f778

# Core dependencies (many are pulled by whisperx-mlx, but we list explicitly for clarity)
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# MLX for Apple Silicon GPU acceleration
mlx>=0.5.0
mlx-whisper>=0.4.0
